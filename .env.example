# FastAPI 后端的基础地址，本地默认指向 127.0.0.1:8000。
# 复制本文件为 .env.local 并按需修改；注意不要以斜杠结尾。
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000

# 旧版后台（Jinja2）的基础地址，过渡期内部分链接会跳回旧后台。
# 留空时默认等于 NEXT_PUBLIC_API_BASE_URL；同样不要以斜杠结尾。
NEXT_PUBLIC_LEGACY_ADMIN_BASE_URL=
